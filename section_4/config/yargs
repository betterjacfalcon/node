const argv = require ('yargs')
        .option('v', {
            alias: 'valor',
            type: 'number',
            demandOption: true,
            describe: 'La valor es la base para la tabla de multiplicar'
        })
        .option('l', {
            alias: 'listar',
            type: 'boolean',
            demandOption: true,
            default: false,
            describe: '/muestra la tabla en consola si listar es true'
        })
        .option('h', {
            alias: 'hasta',
            type: 'number',
            default: 10,          
            describe: '/Numero final para ejecutar la multiplicacion'
        })
        .check((argv, option) =>{
            if (isNaN(argv.v)){
            throw 'La base tiene que ser un numero'
            }
            return true
        })
        .argv;

 module.exports = argv;