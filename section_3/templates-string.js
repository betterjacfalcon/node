const nombre = 'peter';
const real = 'carvajal';

const normal = nombre + ' ' + real;
const template = `${nombre} ${real}`;
const html =
 `<h1> hola mundo</h1>                
 <p>Hola mundo otra vez</p>`;

console.log(normal);
console.log(template);
console.log(html);